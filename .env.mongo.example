# --- MONGODB ENVIRONMENT SETTINGS ---
# Use this when running with APP_STORAGE=mongo

# Application Storage Mode (mongo or mysql)
APP_STORAGE=mongo
SPRING_PROFILES_ACTIVE=mongo

# MongoDB Credentials
MONGO_ROOT_USERNAME=root
MONGO_ROOT_PASSWORD=SDxcieTer385-4R
MONGO_DATABASE=shift_happens_mongo

# MySQL Credentials (still needed for other models)
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=shift_happens
LOCAL_DB_USERNAME=root
APP_DB_USER=app_user
APP_DB_PASSWORD=examplepassword1234!"$
