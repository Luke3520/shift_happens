# ========================
# Application Settings
# ========================
spring.application.name=shift-happens

# ========================
# Datasource / MySQL
# ========================
spring.datasource.url=jdbc:mysql://tramway.proxy.rlwy.net:22651/railway?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=SmnSLziFSobAfxZqfhHEiazxPelPPCGL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hikari Connection Pool Settings
spring.datasource.hikari.pool-name=HikariPool-ShiftHappens
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.max-lifetime=1800000

# ========================
# JPA / Hibernate
# ========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Optional: enable SQL comments in Hibernate logs
spring.jpa.properties.hibernate.use_sql_comments=true

# ========================
# Logging
# ========================
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
